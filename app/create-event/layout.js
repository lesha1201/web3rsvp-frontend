export const metadata = {
  title: "Create your event | web3rsvp",
  description: "Create your virtual event on the blockchain",
};

export default function CreateEventLayout({ children }) {
  return (
    <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">{children}</div>
  );
}
